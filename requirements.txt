# Autonomous Code Generation - Dependencies

# GitHub API
PyGithub>=2.1.1

# Bitbucket API (uses Atlassian REST API)
atlassian-python-api>=3.41.0

# LLM: multi-provider (OpenAI, Anthropic, Gemini) via LiteLLM
litellm>=1.30.0

# LLM: Azure OpenAI via LangChain
langchain-openai>=0.1.0

# Configuration parsing
# configparser is built-in for .ini files

# HTTP requests
requests>=2.31.0

# Activity indicators (spinner, progress, status)
rich>=13.7.0

# Optional: similarity search (pip install sentence-transformers)
# sentence-transformers>=2.2.0

# Optional: Java call graph + symbol extraction (pip install javalang)
# javalang>=0.13.0

# Optional: BM25 keyword search for hybrid retrieval (pip install rank-bm25)
# rank-bm25>=0.2.2

# Optional: AWS OpenSearch knowledge backend
# opensearch-py>=2.4.0
# boto3>=1.34.0
# requests-aws4auth>=1.2.3

# Web platform
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
sqlalchemy>=2.0.0
aiofiles>=23.2.0
